import { WelcomeScreen } from '@/features/auth/WelcomeScreen';

// Auth landing (frame 282:5467): hero + copy + sign-in options.
export default function WelcomeRoute() {
  return <WelcomeScreen />;
}
