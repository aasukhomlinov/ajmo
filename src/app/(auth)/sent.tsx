import { CodeEntryScreen } from '@/features/auth/CodeEntryScreen';

// Auth · Enter code: verify the 6-digit code emailed by the email screen.
export default function CodeEntryRoute() {
  return <CodeEntryScreen />;
}
