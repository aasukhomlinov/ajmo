import { LinkExpiredScreen } from '@/features/auth/LinkExpiredScreen';

// Auth · Link expired (frame 274:1430): failed magic-link callback.
export default function LinkExpiredRoute() {
  return <LinkExpiredScreen />;
}
