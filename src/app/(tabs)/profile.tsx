import { TabPlaceholder } from '@/features/dev/TabPlaceholder';

// Tab shell only — real profile comes in the screen phase.
export default function ProfileScreen() {
  return <TabPlaceholder title="Profile" subtitle="Profile & settings — coming in the screen phase" />;
}
