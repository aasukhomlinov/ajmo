import { SearchScreen } from '@/features/search/SearchScreen';

// Search route — Discover's header search button navigates here. Implementation
// lives in the search feature (empty / typing / results / no-results states).
export default function Search() {
  return <SearchScreen />;
}
